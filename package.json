{
    "name": "trichmind-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev:stack": "docker compose -f docker-compose.dev.yml up --build",
        "prod:stack": "docker compose -f docker-compose.yml up --build",
        "stop": "docker compose down",
        "clean": "docker compose down -v --remove-orphans"
    }
}