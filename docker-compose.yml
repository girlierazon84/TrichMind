# -------------------------------------------------------
# TrichMind Full Stack (Production)
# -------------------------------------------------------

services:
  # 🧱 MongoDB Database
  mongo:
    image: mongo:7
    container_name: trichmind-mongo
    ports:
      - "27018:27017"
    volumes:
      - mongo:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # 🤖 FastAPI ML Service
  ml:
    build: ./ml
    container_name: trichmind-ml
    working_dir: /app
    command: uvicorn inference_api:app --host 0.0.0.0 --port 8000
    environment:
      - MODEL_PATH=${MODEL_PATH:-artifacts/training_outputs/best_models/best_relapse_risk_predictor_model.pkl}
      - ENCODER_PATH=${ENCODER_PATH:-artifacts/training_outputs/label_encoder/label_encoder.pkl}
      - CORS_ORIGIN=${CORS_ORIGIN:-http://localhost:5173,http://127.0.0.1:5173}
    ports:
      - "8000:8000" # Expose FastAPI to the host machine
    volumes:
      - ./ml:/app
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import json,urllib.request,sys; d=json.load(urllib.request.urlopen('http://localhost:8000/healthz', timeout=3)); sys.exit(0 if d.get('ok') else 1)"
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 25s
    restart: unless-stopped

  # 🧠 Node.js API Server
  server:
    build: ./server
    container_name: trichmind-server
    working_dir: /app
    # 👇 Smart dev/prod startup logic
    command: >
      sh -c "
      if [ \"$NODE_ENV\" = 'production' ]; then
        echo '🚀 Starting TrichMind Server (PRODUCTION)...';
        npm run build && npm start;
      else
        echo '🧩 Starting TrichMind Server (DEV hot reload)...';
        npm run dev;
      fi
      "
    environment:
      NODE_ENV: production
      SERVER_URL: http://server:8080
      MONGO_URI: mongodb://mongo:27017/trichmind
      JWT_SECRET: ${JWT_SECRET:-super-secret-change-me}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-refresh-secret-change-me}
      PORT: 8080
      # ✅ Use container name (ml) when inside Docker, fallback to host (localhost)
      ML_BASE_URL: http://localhost:8000
      CORS_ORIGIN: http://localhost:5173,http://127.0.0.1:5173
      CLIENT_URL: http://localhost:5173
      SMTP_HOST: ${SMTP_HOST:-smtp.gmail.com}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USER: ${SMTP_USER:-your_email@gmail.com}
      SMTP_PASS: ${SMTP_PASS:-your_app_password}
      RELAPSE_ALERT_THRESHOLD: ${RELAPSE_ALERT_THRESHOLD:-0.7}
    ports:
      - "8080:8080"
    volumes:
      - ./server:/app
      - /app/node_modules
    depends_on:
      mongo:
        condition: service_healthy
      ml:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/api/auth/me || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 25s
    restart: unless-stopped

  # 🌐 Frontend — Vite React (Production)
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
      target: prod
    container_name: trichmind-client
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=production
      - VITE_API_BASE=http://server:8080
      - VITE_APP_NAME=TrichMind
    depends_on:
      server:
        condition: service_healthy
    restart: unless-stopped

volumes:
  mongo:
